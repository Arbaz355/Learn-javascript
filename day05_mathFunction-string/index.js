// round, ceil, floor, trunc, random, max, min, toFixed(n), parseFloat, parseInt
// pow, sqrt, sin, cos, tan

// String methods -> toString(), String, toUpperCase(), toLowerCase, charAt, length, split, replace, trim, concat, toLocaleString()

const amount = 100000000;
console.log(amount.toLocaleString("hi-IN"));